import React from 'react';
import { useMutation } from '@apollo/client';

// This page needs to be written (obviously).  Just creating this so I could create the profile.js. - Dave
// Need to map over the exercises and render them on the page.
const ExerciseList = () => {
    return (
        <div className="container">
            A nice Homepage!
            </div>
    );
  };

  export default ExerciseList;